//print factors of a given number (n).
#include<stdio.h>
main()
{
	int n,i=1;
	printf(" Enter  the n value:");
	scanf("%d",&n);
	printf(" The factor of %d  \n",n);
	while(i<=n)
	{
		if(n%i==0)
		printf(" %d \t",i);
		i++;
	}
}
